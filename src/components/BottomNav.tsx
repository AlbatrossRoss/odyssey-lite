"use client";

type BottomNavProps = {
  activeTab?: "Explore" | "Create" | "Boards" | "Accounts" | "Versions";
  onExploreClick?: () => void;
};

export function BottomNav(_props: BottomNavProps) {
  void _props;

  return null;
}
